# @package hydra.sweeper
params:
  trainer.args.learning_rate: "tag(log, interval(0.1, 0.7))"
  trainer.method_args.cfg.lora_lr: "tag(log, interval(0.01, 1))"
  trainer.method_args.cfg.npo_beta: "tag(log, interval(0.005, 0.5))"
  # trainer.method_args.cfg.retain_momentum: "interval(0.95, 0.99)"
  # trainer.method_args.cfg.n_pcs: "tag(log, range(300, 600))"